@extends('template')
@section('body')
<div class="page-wrapper"><div class="page-content"><div class="card radius-10"><div class="card-header"><h5 class="mb-0">Buyurtma yetkazish</h5></div><div class="card-body">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="table-responsive"><table class="table table-striped align-middle"><thead><tr><th>Order</th><th>Mijoz</th><th>Manzil</th><th>Tracking</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($deliveries as $delivery)<tr><td><a href="{{ route('orders.show',$delivery->order) }}">{{ $delivery->order->order_code }}</a></td><td>{{ $delivery->recipient_name ?: $delivery->order->client?->name }}<small class="d-block text-muted">{{ $delivery->recipient_phone ?: $delivery->order->client?->phone_number }}</small></td><td>{{ $delivery->address }}</td><td>{{ $delivery->tracking_code }}</td><td>{{ $delivery->status }}</td><td>@if($delivery->status === 'sent')<form method="POST" action="{{ route('courier.orders.accept',$delivery) }}" class="d-inline">@csrf<button class="btn btn-sm btn-success">Qabul qilish</button></form>@elseif(in_array($delivery->status,['accepted','picked_up'],true))<form method="POST" action="{{ route('courier.orders.deliver',$delivery) }}" class="d-inline">@csrf<button class="btn btn-sm btn-primary">Yetkazildi</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Faol delivery yo‘q.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
