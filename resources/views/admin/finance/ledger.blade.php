@extends('template')

@section('style')
<style>
    .ledger-page { padding: 26px; color: #1b2c49; }
    .ledger-hero, .ledger-panel { border: 1px solid #dce7f5; border-radius: 18px; background: #fff; box-shadow: 0 7px 20px rgba(31, 63, 110, .06); }
    .ledger-hero { display: flex; align-items: center; justify-content: space-between; gap: 22px; padding: 26px 29px; background: linear-gradient(120deg, #fff, #f2f7ff); }
    .ledger-eyebrow { margin: 0 0 5px; color: #6f7e97; font-size: .72rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
    .ledger-hero h1 { margin: 0; color: #142f5d; font-size: 1.5rem; font-weight: 800; }
    .ledger-hero p { margin: 7px 0 0; color: #667793; }
    .ledger-links { display: flex; flex-wrap: wrap; gap: 8px; justify-content: flex-end; }
    .ledger-link, .ledger-primary, .ledger-muted-btn { display: inline-flex; align-items: center; justify-content: center; gap: 6px; min-height: 40px; padding: 0 13px; border: 1px solid #c8d8eb; border-radius: 9px; background: #fff; color: #25518b; font-size: .8rem; font-weight: 800; text-decoration: none; }
    .ledger-link--warn { border-color: #ffd0d9; color: #be2447; background: #fff8fa; }
    .ledger-primary { border: 0; background: #176fc7; color: #fff; }
    .ledger-muted-btn { color: #46638d; }
    .ledger-stats { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 14px; margin: 19px 0; }
    .ledger-stat { display: flex; align-items: center; gap: 12px; padding: 16px; border: 1px solid #dfe8f5; border-radius: 15px; background: #fff; box-shadow: 0 5px 16px rgba(31,63,110,.05); }
    .ledger-stat i { display: grid; place-items: center; flex: 0 0 39px; width: 39px; height: 39px; border-radius: 11px; color: #1871cb; background: #eef5ff; font-size: 1.15rem; }
    .ledger-stat--paid i { color: #16894f; background: #edfcf4; }.ledger-stat--refund i { color: #c6284c; background: #fff0f4; }.ledger-stat--debt i { color: #a8660c; background: #fff6e7; }
    .ledger-stat span { display: block; color: #71809a; font-size: .74rem; font-weight: 700; }.ledger-stat strong { display: block; margin-top: 3px; color: #17396e; font-size: 1rem; line-height: 1.15; }
    .ledger-filter { padding: 19px; }.ledger-filter h2, .ledger-panel-head h2, .ledger-cash h2 { display: flex; align-items: center; gap: 8px; margin: 0; color: #17345f; font-size: 1rem; font-weight: 800; }.ledger-filter h2 { margin-bottom: 14px; }.ledger-filter h2 i, .ledger-cash h2 i { color: #e51f4b; }
    .ledger-filter form { display: grid; grid-template-columns: .8fr .8fr 1fr 1fr 1fr 1.35fr; gap: 11px; align-items: end; }.ledger-field { display: grid; gap: 6px; }.ledger-field label, .ledger-cash label { color: #526a8d; font-size: .74rem; font-weight: 800; }.ledger-field input, .ledger-field select, .ledger-cash input, .ledger-cash select { width: 100%; height: 41px; border: 1px solid #cedbeb; border-radius: 9px; padding: 0 10px; background: #fbfdff; color: #273e60; outline: none; }.ledger-field input:focus, .ledger-field select:focus, .ledger-cash input:focus, .ledger-cash select:focus { border-color: #1e7fdd; box-shadow: 0 0 0 3px rgba(30,127,221,.12); background: #fff; }
    .ledger-search { display: flex; gap: 7px; }.ledger-search input { min-width: 0; }.ledger-search button { width: 41px; border: 0; border-radius: 9px; color: #fff; background: #176fc7; }.ledger-search .ledger-muted-btn { flex: 0 0 auto; }
    .ledger-grid { display: grid; grid-template-columns: minmax(0, 1.6fr) minmax(330px, .9fr); gap: 18px; margin-top: 19px; }.ledger-panel { overflow: hidden; }.ledger-panel-head { display: flex; align-items: flex-end; justify-content: space-between; gap: 12px; padding: 18px 20px 15px; border-bottom: 1px solid #e8eef7; }.ledger-panel-head p { margin: 4px 0 0; color: #748199; font-size: .8rem; }.ledger-count { color: #53709b; font-size: .78rem; font-weight: 700; white-space: nowrap; }
    .ledger-table-wrap { overflow-x: auto; }.ledger-table { width: 100%; min-width: 850px; border-collapse: separate; border-spacing: 0; }.ledger-table th { padding: 11px 12px; border-bottom: 1px solid #e2eaf5; color: #60718c; background: #f7faff; font-size: .71rem; font-weight: 800; letter-spacing: .03em; text-align: left; text-transform: uppercase; }.ledger-table td { padding: 13px 12px; border-bottom: 1px solid #edf1f7; color: #344863; font-size: .83rem; vertical-align: middle; }.ledger-table tbody tr:hover { background: #fbfdff; }.ledger-code { display: block; color: #193762; font-size: .84rem; font-weight: 800; }.ledger-small { display: block; margin-top: 2px; color: #7e8ca2; font-size: .74rem; }.ledger-proof { color: #1d71c9; font-size: .74rem; font-weight: 800; text-decoration: none; }.ledger-money { color: #173d74; font-weight: 800; white-space: nowrap; }.ledger-status { display: inline-flex; align-items: center; gap: 5px; padding: 5px 8px; border-radius: 999px; font-size: .72rem; font-weight: 800; white-space: nowrap; }.status-confirmed, .status-partially_refunded { color: #167648; background: #edfcf4; }.status-pending { color: #a6640b; background: #fff5df; }.status-cancelled, .status-refunded { color: #bf2547; background: #fff0f3; }
    .ledger-actions { display: flex; justify-content: flex-end; flex-wrap: wrap; gap: 6px; }.ledger-action { min-height: 32px; border: 1px solid #c9d9ec; border-radius: 8px; padding: 6px 8px; color: #25518b; background: #fff; font-size: .73rem; font-weight: 800; }.ledger-action--approve { border-color: #bee4d0; color: #167648; background: #f4fcf7; }.ledger-action--cancel { border-color: #ffd0d9; color: #c61d43; background: #fff8fa; }.ledger-action--refund { border-color: #ffe0a8; color: #a6640b; background: #fffaf0; }.ledger-refund { width: 220px; margin-top: 7px; padding: 9px; border: 1px solid #dce6f3; border-radius: 9px; background: #f8fbff; }.ledger-refund input { width: 100%; margin-bottom: 6px; border: 1px solid #cedbeb; border-radius: 7px; padding: 7px; font-size: .75rem; }
    .ledger-side { display: grid; align-content: start; gap: 18px; }.ledger-cash { padding: 19px; }.ledger-cash p { margin: 5px 0 16px; color: #748199; font-size: .8rem; }.ledger-cash-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 11px; }.ledger-cash-field { display: grid; gap: 6px; }.ledger-cash-submit { grid-column: 1 / -1; min-height: 41px; border: 0; border-radius: 9px; background: #176fc7; color: #fff; font-weight: 800; }.ledger-sessions { display: grid; }.ledger-session { display: grid; grid-template-columns: 1fr auto; gap: 10px; padding: 14px 18px; border-bottom: 1px solid #edf1f7; }.ledger-session:last-child { border-bottom: 0; }.ledger-session strong { display: block; color: #1c3b69; font-size: .84rem; }.ledger-session-side { text-align: right; }.ledger-session-state { display: inline-flex; padding: 4px 7px; border-radius: 999px; color: #47648b; background: #edf4fc; font-size: .7rem; font-weight: 800; }.ledger-session-state--done { color: #167648; background: #edfcf4; }.ledger-session-state--closed { color: #68798f; background: #f0f3f7; }.ledger-session details { margin-top: 6px; }.ledger-session summary { color: #2862a5; cursor: pointer; font-size: .75rem; font-weight: 800; }.ledger-session form { width: 190px; margin: 7px 0 0 auto; }.ledger-session form input { width: 100%; margin-bottom: 5px; border: 1px solid #cedbeb; border-radius: 7px; padding: 6px; font-size: .74rem; }
    .ledger-debt { margin-top: 18px; }.ledger-debt-link { color: #1b68bd; font-weight: 800; text-decoration: none; }.ledger-debt-money { color: #c02043; font-weight: 800; }.ledger-pagination { padding: 15px 18px; border-top: 1px solid #edf1f7; }.ledger-empty { padding: 38px 18px; color: #75839a; text-align: center; }.ledger-empty i { display: block; margin-bottom: 7px; color: #e51f4b; font-size: 1.7rem; }
    .ledger-cash-note { display: flex; align-items: flex-start; gap: 11px; padding: 13px; border: 1px solid #d7e5f6; border-radius: 11px; background: #f7fbff; }.ledger-cash-note i { display: grid; place-items: center; flex: 0 0 30px; width: 30px; height: 30px; border-radius: 50%; color: #176fc7; background: #e5f1ff; font-size: 1rem; }.ledger-cash-note strong { display: block; color: #244b7b; font-size: .82rem; }.ledger-cash-note span { display: block; margin-top: 3px; color: #6e7f98; font-size: .76rem; line-height: 1.45; }
    @media(max-width: 1260px) { .ledger-filter form { grid-template-columns: repeat(3, 1fr); }.ledger-grid { grid-template-columns: 1fr; }.ledger-side { grid-template-columns: repeat(2, minmax(0, 1fr)); align-items: start; } } @media(max-width: 700px) { .ledger-page { padding: 16px; }.ledger-hero { align-items: flex-start; flex-direction: column; padding: 22px; }.ledger-links { width: 100%; }.ledger-link { flex: 1; }.ledger-stats, .ledger-filter form, .ledger-side, .ledger-cash-grid { grid-template-columns: 1fr; }.ledger-cash-submit { grid-column: auto; }.ledger-panel-head { align-items: flex-start; flex-direction: column; }.ledger-refund { width: 100%; } }
</style>
@endsection

@section('body')
@php
    $statusLabels = ['pending' => 'Kutilmoqda', 'confirmed' => 'Tasdiqlangan', 'partially_refunded' => 'Qisman qaytarilgan', 'refunded' => 'Qaytarilgan', 'cancelled' => 'Bekor qilingan'];
    $typeLabels = ['cash' => 'Naqd', 'card' => 'Karta', 'online' => 'Onlayn', 'transfer' => 'O‘tkazma', 'admin_entry' => 'Administrator'];
    $hasFilters = ($data['date_from'] ?? null) || ($data['date_to'] ?? null) || ($data['filial_id'] ?? null) || ($data['status'] ?? null) || ($data['payment_type'] ?? null) || ($data['q'] ?? null);
@endphp
<div class="page-wrapper"><main class="ledger-page">
    @if(session('success'))<div class="alert alert-success mb-3">{{ session('success') }}</div>@endif
    @if($errors->any())<div class="alert alert-danger mb-3">{{ $errors->first() }}</div>@endif

    <section class="ledger-hero"><div><p class="ledger-eyebrow">Moliyaviy boshqaruv</p><h1>To‘lovlar va kassa reyestri</h1><p>To‘lovlar, qaytarimlar, kassa smenalalari va qarzdor buyurtmalarni kuzating.</p></div><div class="ledger-links">@if(auth()->user()->hasAnyRole(['admin_manager','super_admin']))<a class="ledger-link" href="{{ route('finance.dashboard') }}"><i class='bx bx-line-chart'></i> Moliya boshqaruvi</a><a class="ledger-link ledger-link--warn" href="{{ route('finance.margin-leaks.index') }}"><i class='bx bx-error-circle'></i> Marja xavflari</a>@endif</div></section>

    <section class="ledger-stats"><article class="ledger-stat"><i class='bx bx-receipt'></i><span>Jami to‘lov<strong>{{ number_format($summary['gross'], 0, ',', ' ') }} so‘m</strong></span></article><article class="ledger-stat ledger-stat--paid"><i class='bx bx-check-circle'></i><span>Sof tushum<strong>{{ number_format($summary['effective'], 0, ',', ' ') }} so‘m</strong></span></article><article class="ledger-stat ledger-stat--refund"><i class='bx bx-undo'></i><span>Qaytarimlar<strong>{{ number_format($summary['refunds'], 0, ',', ' ') }} so‘m</strong></span></article><article class="ledger-stat ledger-stat--debt"><i class='bx bx-wallet'></i><span>Qarzdorlik<strong>{{ number_format($debtTotal, 0, ',', ' ') }} so‘m</strong></span></article></section>

    <section class="ledger-panel ledger-filter"><h2><i class='bx bx-filter-alt'></i> To‘lovlarni filtrlash</h2><form method="GET" action="{{ route('finance.ledger.index') }}"><div class="ledger-field"><label for="from">Dan</label><input id="from" type="date" name="date_from" value="{{ $data['date_from'] ?? '' }}"></div><div class="ledger-field"><label for="to">Gacha</label><input id="to" type="date" name="date_to" value="{{ $data['date_to'] ?? '' }}"></div><div class="ledger-field"><label for="branch">Filial</label><select id="branch" name="filial_id"><option value="">Barcha filiallar</option>@foreach($filials as $filial)<option value="{{ $filial->id }}" @selected((string)($data['filial_id'] ?? '') === (string)$filial->id)>{{ $filial->name }}</option>@endforeach</select></div><div class="ledger-field"><label for="status">Holat</label><select id="status" name="status"><option value="">Barchasi</option>@foreach($paymentStatuses as $status)<option value="{{ $status }}" @selected(($data['status'] ?? '') === $status)>{{ $statusLabels[$status] ?? $status }}</option>@endforeach</select></div><div class="ledger-field"><label for="type">To‘lov turi</label><select id="type" name="payment_type"><option value="">Barchasi</option>@foreach($paymentTypes as $type)<option value="{{ $type }}" @selected(($data['payment_type'] ?? '') === $type)>{{ $typeLabels[$type] ?? $type }}</option>@endforeach</select></div><div class="ledger-field"><label for="search">Qidiruv</label><div class="ledger-search"><input id="search" name="q" value="{{ $data['q'] ?? '' }}" placeholder="Chek yoki buyurtma"><button type="submit"><i class='bx bx-search'></i></button>@if($hasFilters)<a class="ledger-muted-btn" href="{{ route('finance.ledger.index') }}">Tozalash</a>@endif</div></div></form></section>

    <section class="ledger-grid"><section class="ledger-panel"><div class="ledger-panel-head"><div><h2>To‘lovlar reyestri</h2><p>Cheklar, kassir, holat va qaytarimlar.</p></div><span class="ledger-count">{{ $payments->total() }} ta to‘lov</span></div><div class="ledger-table-wrap"><table class="ledger-table"><thead><tr><th>Chek / sana</th><th>Buyurtma / mijoz</th><th>Filial</th><th>To‘lov turi</th><th>Holat</th><th>Summa</th><th>Amallar</th></tr></thead><tbody>@forelse($payments as $payment)@php($paymentStatus = $payment->status ?: 'pending')<tr><td><span class="ledger-code">{{ $payment->receipt_number ?: 'Chek raqami yo‘q' }}</span><small class="ledger-small">{{ optional($payment->created_at)->format('d.m.Y H:i') }}</small>@if($payment->payment_proof_path)<a class="ledger-proof" href="{{ route('finance.ledger.proof', $payment) }}">Kvitansiya</a>@endif</td><td><span class="ledger-code">{{ $payment->order?->order_code ?: $payment->document?->document_code ?: 'Eski yozuv' }}</span><small class="ledger-small">{{ $payment->order?->client?->name ?: 'Mijoz ko‘rsatilmagan' }}</small></td><td>{{ $payment->filial?->name ?: $payment->order?->filial?->name ?: '—' }}</td><td>{{ $typeLabels[$payment->payment_type] ?? $payment->payment_type }}<small class="ledger-small">{{ $payment->cashier?->name ?: 'Kassir ko‘rsatilmagan' }}</small></td><td><span class="ledger-status status-{{ $paymentStatus }}">{{ $statusLabels[$paymentStatus] ?? $paymentStatus }}</span>@if($payment->refund_amount > 0)<small class="ledger-small text-danger">Qaytarim: {{ number_format($payment->refund_amount,0,',',' ') }}</small>@endif</td><td><span class="ledger-money">{{ number_format($payment->effective_amount, 0, ',', ' ') }} so‘m</span><small class="ledger-small">Jami: {{ number_format($payment->amount, 0, ',', ' ') }}</small></td><td><div class="ledger-actions">@if($paymentStatus === 'pending')<form method="POST" action="{{ route('finance.ledger.confirm', $payment) }}">@csrf<button class="ledger-action ledger-action--approve">Tasdiqlash</button></form>@endif @if(!in_array($paymentStatus, ['cancelled','refunded']))<form method="POST" action="{{ route('finance.ledger.cancel', $payment) }}" onsubmit="return confirm('To‘lovni bekor qilasizmi?')">@csrf<input type="hidden" name="reason" value="Operator bekor qildi"><button class="ledger-action ledger-action--cancel">Bekor qilish</button></form><details><summary class="ledger-action ledger-action--refund">Qaytarim</summary><form method="POST" action="{{ route('finance.ledger.refund', $payment) }}" class="ledger-refund">@csrf<input type="number" name="amount" min="0.01" max="{{ $payment->effective_amount }}" step="0.01" placeholder="Summa" required><input name="reason" placeholder="Sabab" required><button class="ledger-action ledger-action--refund">Saqlash</button></form></details>@endif</div></td></tr>@empty<tr><td colspan="7"><div class="ledger-empty"><i class='bx bx-receipt'></i><strong>To‘lov topilmadi.</strong><br><span>Filtrlarni o‘zgartirib ko‘ring.</span></div></td></tr>@endforelse</tbody></table></div>@if($payments->hasPages())<div class="ledger-pagination">{{ $payments->links() }}</div>@endif</section>

        <aside class="ledger-side">
            @if(auth()->user()->hasRole('admin_filial'))
                <section class="ledger-panel ledger-cash">
                    <h2><i class='bx bx-wallet-alt'></i> Mening kassa smenam</h2>
                    <p>Siz biriktirilgan filial uchun bugungi naqd pul smenasini ochishingiz mumkin.</p>
                    <form method="POST" action="{{ route('finance.cash-sessions.open') }}" class="ledger-cash-grid">
                        @csrf
                        <div class="ledger-cash-field">
                            <label for="session_branch">Filial</label>
                            <select id="session_branch" name="filial_id" required>
                                @foreach($filials as $filial)<option value="{{ $filial->id }}">{{ $filial->name }}</option>@endforeach
                            </select>
                        </div>
                        <div class="ledger-cash-field"><label for="session_date">Sana</label><input id="session_date" type="date" name="session_date" value="{{ today()->toDateString() }}"></div>
                        <div class="ledger-cash-field"><label for="balance">Boshlang‘ich qoldiq</label><input id="balance" type="number" name="opening_balance" min="0" step="0.01" value="0" required></div>
                        <button class="ledger-cash-submit" type="submit">Smenani ochish</button>
                    </form>
                </section>
            @else
                <section class="ledger-panel ledger-cash">
                    <h2><i class='bx bx-shield-quarter'></i> Kassa nazorati</h2>
                    <p>Kassa smenalari bu yerda kuzatuv va tekshiruv uchun ko‘rsatiladi.</p>
                    <div class="ledger-cash-note">
                        <i class='bx bx-lock-alt'></i>
                        <div><strong>Faqat filial administratori</strong><span>Smenani ochish va yopish amallarini faqat o‘z filialiga biriktirilgan filial administratori bajaradi.</span></div>
                    </div>
                </section>
            @endif
            <section class="ledger-panel">
                <div class="ledger-panel-head"><div><h2>Kassa smenalalari</h2><p>So‘nggi 30 ta smena.</p></div><span class="ledger-count">{{ $sessions->count() }} ta</span></div>
                <div class="ledger-sessions">
                    @forelse($sessions as $session)
                        <article class="ledger-session">
                            <div>
                                <strong>{{ $session->filial?->name ?: 'Filial ko‘rsatilmagan' }}</strong>
                                <small class="ledger-small">{{ $session->cashier?->name ?: 'Kassir ko‘rsatilmagan' }} · {{ optional($session->session_date)->format('d.m.Y') }}</small>
                                <small class="ledger-small {{ (float)$session->variance == 0 ? 'text-success' : 'text-danger' }}">Farq: {{ number_format($session->variance,0,',',' ') }} so‘m</small>
                            </div>
                            <div class="ledger-session-side">
                                <span class="ledger-session-state {{ $session->status === 'reconciled' ? 'ledger-session-state--done' : ($session->status === 'closed' ? 'ledger-session-state--closed' : '') }}">{{ $session->status === 'reconciled' ? 'Moslashtirilgan' : ($session->status === 'open' ? 'Ochiq' : 'Yopilgan') }}</span>
                                <small class="ledger-small">{{ $session->expected_cash !== null ? number_format($session->expected_cash,0,',',' ') : '—' }} / {{ $session->actual_cash !== null ? number_format($session->actual_cash,0,',',' ') : '—' }}</small>
                                @if($session->status === 'open' && auth()->user()->hasRole('admin_filial') && (int) $session->cashier_id === (int) auth()->id())
                                    <details><summary>Smenani yopish</summary><form method="POST" action="{{ route('finance.cash-sessions.close', $session) }}">@csrf<input type="number" name="actual_cash" min="0" step="0.01" placeholder="Amaldagi kassa" required><input name="notes" placeholder="Izoh"><button class="ledger-action">Yopish</button></form></details>
                                @elseif($session->status === 'closed' && auth()->user()->hasRole('admin_manager'))
                                    <form method="POST" action="{{ route('finance.cash-sessions.reconcile', $session) }}">@csrf<button class="ledger-action ledger-action--approve">Moslashtirish</button></form>
                                @endif
                            </div>
                        </article>
                    @empty
                        <div class="ledger-empty"><i class='bx bx-wallet'></i><strong>Kassa smenasi yo‘q.</strong></div>
                    @endforelse
                </div>
            </section>
        </aside>
    </section>

    <section class="ledger-panel ledger-debt"><div class="ledger-panel-head"><div><h2>Qarzdor buyurtmalar</h2><p>To‘liq to‘lanmagan faol buyurtmalar.</p></div><span class="ledger-count">{{ $debt->count() }} ta buyurtma</span></div><div class="ledger-table-wrap"><table class="ledger-table"><thead><tr><th>Buyurtma</th><th>Mijoz</th><th>Filial</th><th>Jami</th><th>To‘langan</th><th>Qoldiq</th></tr></thead><tbody>@forelse($debt as $order)<tr><td><a class="ledger-debt-link" href="{{ route('orders.show', $order) }}">{{ $order->order_code }}</a></td><td>{{ $order->client?->name ?: '—' }}<small class="ledger-small">{{ $order->client?->phone_number }}</small></td><td>{{ $order->filial?->name ?: '—' }}</td><td>{{ number_format($order->total_amount,0,',',' ') }} so‘m</td><td>{{ number_format($order->paid_amount,0,',',' ') }} so‘m</td><td><span class="ledger-debt-money">{{ number_format($order->balance_amount,0,',',' ') }} so‘m</span></td></tr>@empty<tr><td colspan="6"><div class="ledger-empty"><i class='bx bx-check-double'></i><strong>Qarzdorlik yo‘q.</strong></div></td></tr>@endforelse</tbody></table></div></section>
</main></div>
@endsection
