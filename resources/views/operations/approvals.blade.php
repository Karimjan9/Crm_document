@extends('template')
@section('body')
<div class="page-wrapper"><div class="page-content"><h4 class="mb-4">Tasdiqlash markazi</h4><div class="card shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Turi</th><th>So‘rovchi</th><th>Summa / margin</th><th>Sabab</th><th>Status</th><th></th></tr></thead><tbody>@forelse($approvals as $approval)<tr><td><strong>{{ $approval->type }}</strong><small class="d-block text-muted">#{{ $approval->id }}</small></td><td>{{ $approval->requestedBy?->name }}</td><td>{{ $approval->amount ? number_format($approval->amount,0,',',' ') : '—' }}<small class="d-block">{{ $approval->projected_margin_percent !== null ? $approval->projected_margin_percent.'%' : '' }}</small></td><td>{{ $approval->reason }}</td><td><span class="badge text-bg-{{ $approval->status==='pending'?'warning':($approval->status==='approved'?'success':'secondary') }}">{{ $approval->status }}</span></td><td>@if($approval->status==='pending')<form method="POST" action="{{ route('operations.approvals.resolve',$approval) }}" class="d-flex gap-1">@csrf<select name="decision" class="form-select form-select-sm"><option value="approved">Tasdiqlash</option><option value="rejected">Rad etish</option></select><button class="btn btn-sm btn-primary">Yuborish</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted p-5">Tasdiqlar yo‘q.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $approvals->links() }}</div></div></div></div>
@endsection
